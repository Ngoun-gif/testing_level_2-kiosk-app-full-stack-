# IDE
.idea/

# PyInstaller / build outputs
dist/
build/
build_app.txt
*.spec

# AdminLTE vendor dist inside dashboard (if you don't want to track vendor files)
dashboard/assets/dist/


# Build / runtime artifacts

identifier.sqlite

# PyInstaller specs
*.spec

# Local entry scripts (if intentionally local-only)
dashboard_main.py
kiosk_main.py

# Python dependencies (ONLY if you really want local-only)
requirements.txt
